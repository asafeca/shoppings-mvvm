<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.laas.shoppingsmvvm">

    <application
        android:name=".di.ShoppingsApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShoppingsMVVM">
        <activity
            android:name=".presentation.DetailsActivity"
            android:theme="@style/Theme.ShoppingsMVVM.NoActionBar"
            android:exported="false" />
        <activity
            android:name=".presentation.AppLoginActivity"
            android:exported="true"
            android:label="@string/title_activity_app_login"
            android:theme="@style/Theme.ShoppingsMVVM.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            />
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ShoppingsMVVM.NoActionBar"></activity>
    </application>

</manifest>